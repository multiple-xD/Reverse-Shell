################################################
                Reverse Shell
################################################

This simple app allows a server program to run commands on a remote client
The server program is accessed via a GUI created in PyQt5
Run the main.py script to start the server GUI
Run the client.py script to connect to the server
NOTE - Before running the client change the hardcoded IP in the script to your own machine's IP
